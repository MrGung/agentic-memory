# Repository Instructions

This is a Babashka/Clojure project. When suggesting code:
- Use Babashka-compatible syntax
- Prefer `babashka.process/sh` over Java interop for shell commands
- Use EDN for data serialization
- Keep functions small and pure where possible
